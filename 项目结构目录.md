# NovelToScript 项目目录结构

## 项目概述

NovelToScript 是一个 AI 辅助小说转剧本工具，帮助作者将小说改编为结构化 YAML 剧本。

## 目录结构树

```
novelToScript/
├── .env                          # 环境配置文件（本地配置，已忽略提交）
├── .env.example                  # 环境配置示例文件
├── .gitignore                    # Git 忽略规则
├── docker-compose.yml            # Docker Compose 编排配置
├── README.md                     # 项目说明文档
├── YAML_SCHEMA.md                # YAML Schema 定义文档
│
├── backend/                      # 后端服务（Python FastAPI）
│   ├── Dockerfile                # 后端 Docker 镜像构建文件
│   ├── README.md                 # 后端说明文档
│   ├── pyproject.toml            # Python 项目配置
│   ├── data/                     # 后端数据目录
│   │   ├── noveltoscript.db      # SQLite 数据库文件
│   │   └── user_settings.json    # 用户设置文件
│   │
│   ├── app/                      # 应用主目录
│   │   ├── __init__.py
│   │   ├── main.py               # FastAPI 应用入口
│   │   ├── config.py             # 应用配置
│   │   ├── database.py           # 数据库模块
│   │   │
│   │   ├── api/                  # API 路由层
│   │   │   ├── __init__.py
│   │   │   ├── conversion.py     # 转换相关 API
│   │   │   ├── export.py         # 导出相关 API
│   │   │   ├── projects.py       # 项目管理 API
│   │   │   ├── scripts.py        # 剧本管理 API
│   │   │   └── settings.py       # 设置相关 API
│   │   │
│   │   ├── domain/               # 领域模型层
│   │   │   ├── __init__.py
│   │   │   ├── models.py         # 数据模型定义
│   │   │   └── script_schema.py  # 剧本 Schema 定义
│   │   │
│   │   ├── infrastructure/       # 基础设施层
│   │   │   ├── __init__.py
│   │   │   ├── database.py       # 数据库连接
│   │   │   ├── file_storage.py   # 文件存储
│   │   │   ├── llm_client.py     # LLM 客户端
│   │   │   └── task_runner.py    # 任务执行器
│   │   │
│   │   ├── prompts/              # LLM 提示词模板
│   │   │   ├── analyze.j2        # 分析提示词
│   │   │   ├── convert.j2        # 转换提示词
│   │   │   └── extract.j2        # 提取提示词
│   │   │
│   │   └── services/             # 业务服务层
│   │       ├── __init__.py
│   │       ├── conversion_engine.py    # 转换引擎
│   │       ├── element_extractor.py    # 元素提取器
│   │       ├── export_service.py       # 导出服务
│   │       ├── import_parser.py        # 导入解析器
│   │       └── yaml_handler.py         # YAML 处理器
│   │
│   └── tests/                    # 测试目录
│       ├── test_api.py                # API 测试
│       ├── test_conversion_engine.py   # 转换引擎测试
│       ├── test_core.py               # 核心功能测试
│       ├── test_import_parser.py       # 导入解析器测试
│       └── test_yaml_handler.py        # YAML 处理器测试
│
├── frontend/                     # 前端服务（React + TypeScript + Vite）
│   ├── Dockerfile                # 前端 Docker 镜像构建文件
│   ├── index.html                # HTML 入口文件
│   ├── package.json              # NPM 依赖配置
│   ├── package-lock.json         # NPM 依赖锁定文件
│   ├── tsconfig.json             # TypeScript 配置
│   ├── tsconfig.node.json        # Node TypeScript 配置
│   ├── vite.config.ts            # Vite 构建配置
│   │
│   ├── dist/                     # 构建输出目录
│   │   ├── index.html
│   │   └── assets/               # 静态资源
│   │       ├── index-CP1ciHE3.js
│   │       └── index-DlagRO6U.css
│   │
│   └── src/                      # 源代码目录
│       ├── main.tsx              # 应用入口
│       ├── App.tsx               # 根组件
│       ├── index.css             # 全局样式
│       │
│       ├── pages/                # 页面组件
│       │   ├── Conversion.tsx    # 转换页面
│       │   ├── Editor.tsx        # 编辑器页面
│       │   ├── Export.tsx        # 导出页面
│       │   ├── Import.tsx        # 导入页面
│       │   ├── ProjectList.tsx   # 项目列表页面
│       │   ├── ProjectWorkspace.tsx  # 项目工作区页面
│       │   └── Settings.tsx      # 设置页面
│       │
│       ├── services/             # 服务层
│       │   └── api.ts            # API 调用封装
│       │
│       └── types/                # 类型定义
│           └── index.ts          # 全局类型定义
│
├── data/                         # 项目数据目录
│   ├── noveltoscript.db          # SQLite 数据库（运行时生成）
│   └── 小说test.txt              # 测试小说文件
│
├── docs/                         # 项目文档
│   ├── DEVELOPMENT_DESIGN.md     # 开发设计文档
│   ├── IMPLEMENTATION.md         # 实施记录文档
│   └── YAML_SCHEMA.md            # YAML Schema 定义文档
│
├── samples/                      # 示例文件
│   └── input/                    # 输入示例
│       └── sample_novel.txt      # 示例小说文件
│
└── schemas/                      # JSON Schema 定义
    └── script.schema.json        # 剧本 Schema 定义
```

## 核心目录说明

### backend/ - 后端服务

基于 Python FastAPI 的后端服务，采用分层架构：

- **api/**: RESTful API 路由层，处理 HTTP 请求
- **domain/**: 领域模型层，定义业务实体和规则
- **infrastructure/**: 基础设施层，处理数据库、文件存储、LLM 调用等
- **services/**: 业务服务层，实现核心业务逻辑
- **prompts/**: Jinja2 模板，用于生成 LLM 提示词
- **tests/**: 单元测试和集成测试

### frontend/ - 前端服务

基于 React + TypeScript + Vite 的前端应用：

- **src/pages/**: 各功能页面组件
- **src/services/**: API 调用封装
- **src/types/**: TypeScript 类型定义
- **dist/**: 构建输出目录

### docs/ - 项目文档

包含项目的设计、实施和 Schema 定义文档。

### samples/ - 示例文件

包含示例输入文件，用于测试和演示。

### schemas/ - Schema 定义

JSON Schema 定义文件，用于数据验证。

## 配置文件说明

### .env / .env.example

环境配置文件，包含：
- LLM 提供商配置（OpenAI 等）
- 转换参数配置
- 性能参数配置
- 开发模式配置

### docker-compose.yml

Docker Compose 编排配置，定义了前后端服务的容器化部署。

### .gitignore

Git 忽略规则，排除：
- Python 缓存文件
- Node 依赖目录
- 构建输出
- 数据库文件
- 环境配置文件
- IDE 配置文件

## 技术栈

- **后端**: Python 3.11+, FastAPI, SQLAlchemy, Jinja2
- **前端**: React, TypeScript, Vite, Monaco Editor
- **数据库**: SQLite
- **AI**: OpenAI API / 其他 LLM 提供商
- **容器化**: Docker, Docker Compose

## 最近更新

- 新增 `YAML_SCHEMA.md` - YAML Schema 定义文档
- 新增 `backend/api/export.py` - 导出相关 API
- 新增 `backend/data/user_settings.json` - 用户设置文件
- 新增 `backend/tests/test_api.py` - API 测试
- 新增 `backend/tests/test_core.py` - 核心功能测试
- 新增 `data/小说test.txt` - 测试小说文件
